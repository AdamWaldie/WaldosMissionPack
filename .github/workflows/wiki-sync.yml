name: Sync wiki to GitHub Wiki

on:
  push:
    branches:
      - main
    paths:
      - 'wiki/**'

jobs:
  sync-wiki:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout main repo
        uses: actions/checkout@v4

      - name: Clone wiki repo
        run: |
          git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git wiki-repo

      - name: Validate wiki assets
        run: python3 releaseVerificationAndDeployment/check_wiki_assets.py

      - name: Validate wiki structure and navigation
        run: python3 releaseVerificationAndDeployment/wiki_style_checker.py

      - name: Synchronize complete wiki tree
        run: |
          rsync -a --delete --exclude='.git/' wiki/ wiki-repo/

      - name: Commit and push if changed
        run: |
          cd wiki-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No wiki changes to push."
          else
            git commit -m "Sync wiki from main [skip ci]"
            git push origin master
          fi
