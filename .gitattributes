# Generated audit mission snapshots must remain byte-for-byte identical to the
# release sources, including legacy whitespace in otherwise unchanged files.
releaseVerificationAndDeployment/fullArmaAudit/WMP_FPA.VR/** -whitespace
