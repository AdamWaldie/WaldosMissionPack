# PR21-PR32 Arma Audit Findings Log

This is the live, evidence-backed defect log for the reusable `WMP Full Arma
Audit` mission. It records player observations separately from automated/static
checks so an automated pass cannot conceal a broken manual workflow.

Status values: `OPEN`, `IN PROGRESS`, `FIXED - RETEST REQUIRED`, `VERIFIED`, or
`EXPECTED - CLARIFIED`.

## Environment and audit harness

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| ENV-001 | Audit launches must use CBA, ACE, ZEN and ACRE2. | The 2026-07-26 16:04 RPT records CBA 3.18.6, ACE 3.21.0, ZEN 1.15.1 and ACRE2 2.14.0 loaded. The core console now exposes an explicit required-mod status action. | FIXED - RETEST REQUIRED |
| ENV-002 | Client file patching caused a multiplayer signature/rejection path. | Reported during the first dedicated/client harness. The hosted editor-style audit is now the primary manual path; copied mission-local scripts remove the release-source visibility problem. | FIXED - RETEST REQUIRED |
| ENV-003 | Early generated mission was nearly empty and could not see repository scripts. | Reported in-game. The generator now stages the complete `MissionScripts`, pictures, economy config, insignias and release entry-file snapshot plus a walkable feature range. Build validation compares the packaged function tree with source. | FIXED - RETEST REQUIRED |
| ENV-004 | Audit must be repeatable without an over-engineered server harness. | Hosted multiplayer launcher and reusable mission folder added. Dedicated mode remains a targeted authority/JIP test. | FIXED - RETEST REQUIRED |
| ENV-005 | Zeus module exists but is not assigned to the selected player. | The generated curator now targets `qa_player_1`; runtime assignment is refreshed to the first connected player and the server assertion requires an assigned curator unit. | FIXED - RETEST REQUIRED |
| ENV-006 | Audit mission needs every runtime feature represented by real configured objects/compositions. | The canonical legacy scenario now starts the complete walkable range through production setup functions after full-pack initialization. It creates Audit Control, Mission Flow, logistics, MHQ, VVD, Economy, EW, party, all procedure/difficulty fixtures, live EOD, Zeus and function stations as real multiplayer objects. | FIXED - RETEST REQUIRED |
| ENV-007 | Audit mission must remain editable and unbinarized. | The canonical audit retains the proven plain-text `version=12` VR mission. The failed generated Eden mission is no longer the release gate. Config validation scans `.sqm`, and the builder rejects loss of the legacy mission header. | FIXED - RETEST REQUIRED |
| ENV-008 | The audit bypassed the pack's real startup and therefore omitted or mistimed runtime systems. | The active mission now wraps the current real entry-point contents with pre-configuration and post-start audit hooks. `description.ext` is the current pack and includes the actual `mission.sqm`. The production init also no longer deadlocks while waiting for a nonexistent dedicated-server player. | FIXED - RETEST REQUIRED |
| ENV-009 | Manual hosted audit automatically ran destructive assertions, changed SafeStart, created/deleted network AI and killed the host. | Confirmed in the 22:12 RPT and final debrief screenshot. The builder now has explicit `manual` and `automated` modes. Hosted mode defaults to manual; server/client audit scripts and temporary party AI are gated behind `Waldo_QA_RunAutomation`. | FIXED - RETEST REQUIRED |
| ENV-010 | Static fixture props generated repeated under-terrain/falling warnings. | Placed signs, laptops, tables and equipment now disable simulation in `mission.sqm` and in runtime fixture recovery. Only vehicles and the airborne paradrop aircraft retain simulation. | FIXED - RETEST REQUIRED |
| ENV-011 | The testbed could be assembled differently by each coding agent. | `build_pr_review_audit.py` and `launch_pr_review_audit.ps1` are now canonical. They derive the pack from the release allowlist, retain the proven mission shell, stage the physical range scripts and emit `audit_build_manifest.json`. | FIXED - RETEST REQUIRED |
| ENV-012 | The restored PR-review mission loaded the full pack but remained an empty VR surface with no stations. | The canonical builder had omitted the existing range continuations. It now stages and runs `featureRangeServer.sqf` and `featureRangeClient.sqf` after production startup, creates missing function consoles, assigns a runtime Zeus curator and avoids duplicate MiniGames/Economy initialization. | FIXED - RETEST REQUIRED |
| ENV-013 | Mission selection reported that `Land_Radio_F` no longer exists for four Radio Tune fixtures. | The 2026-07-27 00:51 RPT identifies Items 56-59. The generator and runtime recovery list now use the present vanilla `Land_PortableServer_01_sand_F` field-equipment class, and regression coverage rejects the removed class. | FIXED - RETEST REQUIRED |
| ENV-014 | Entering the audit produced an immediate explosion and five BLUFOR vehicle-loss records. | The 2026-07-27 00:51 RPT records the construction HEMTT, MHQ truck, both convoy MRAPs and EW immune MRAP dying together during startup. All pre-staged objects now begin with simulation disabled; stationary test vehicles remain inert, and the convoy only enables simulation and receives crews after the operator selects `START / RESET CONVOY TEST`. | FIXED - RETEST REQUIRED |
| ENV-015 | Mission header advertised 31 slots while the audit supplies five playable roles. | The same RPT reports the role/header mismatch. Both audit builders now rewrite `Header.maxPlayers` to 5 without changing the release template. | FIXED - RETEST REQUIRED |

## Mission flow, notifications and markers

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| FLOW-001 | SafeStart did not explain protections, confinement, manual state or timer state sufficiently. | Player observation. Persistent SafeStart status was expanded with protections, confinement and countdown/no-timer text. The 16:52 RPT then identified `safeStartNotice.sqf` rejecting Arma structured `TEXT` as `STRING`; the parameter now accepts either and timer arming publishes its deadline before activation. | FIXED - RETEST REQUIRED |
| FLOW-002 | Manually ending SafeStart makes the go-live message disappear immediately. | The go-live transition now uses a dedicated transient control and token rather than the persistent SafeStart control/global hint channel. | FIXED - RETEST REQUIRED |
| FLOW-003 | SafeStart persistent hints could overwrite EW jamming entry/restored notices. | Source confirmed the SafeStart loop previously updated Arma's global hint every second. Dedicated SafeStart and jamming controls have been introduced; in-engine retest remains. | FIXED - RETEST REQUIRED |
| FLOW-004 | ENDEX and AAR should be one understandable flow; its report lacked information and disappeared too quickly. | Player observation. Report content/duration changes are present but need manual verification. | FIXED - RETEST REQUIRED |
| FLOW-005 | Custom 3D waypoints/markers disappeared in the audit mission. | Root cause: audit `initPlayerLocal.sqf` replaced production startup and omitted `Waldo_fnc_Init3DMarkers`. Initial server registry assertion was a false positive. Client initialization/assertion patched; regenerated mission required. | FIXED - RETEST REQUIRED |
| FLOW-006 | Pack should expose a mission-maker API for fully custom 3D world markers. | Requested feature. Create/remove/init functions and documentation are present; client rendering must be verified after mission regeneration. | FIXED - RETEST REQUIRED |
| FLOW-007 | EW jamming messages should use a visible hint-style notice rather than game-chat logging. | Player preference. Dedicated jamming notice control added to avoid SafeStart/global-hint conflict. | FIXED - RETEST REQUIRED |
| FLOW-008 | Active radio jammer produces no visible indicator. | The 17:24 RPT confirms the WEST-only 45 m jammer was registered and the ACRE custom signal function installed, but no client JAMMED transition occurred. Root cause: HUD/RDF/TFAR sampling used ground-level `getPosASL player`; terrain LOS treated the endpoint terrain as an obstruction and returned factor 0. Near-ground LOS endpoints are now lifted 1.5 m, and registry/state diagnostics plus a real near-emitter factor assertion were added. | FIXED - RETEST REQUIRED |
| FLOW-009 | SafeStart countdown shows no ticking timer and automatic completion is unproven. | The 17:24 RPT records two 30-second timers being armed but no completion/cancellation record. Timer setup now reasserts the repeat-safe client HUD even when SafeStart was already active, uses a monotonic token, logs completion/cancellation, and has a real two-second auto-lift assertion. | FIXED - RETEST REQUIRED |
| FLOW-010 | Reset/respawn AAR target action appears to do nothing. | The fixture silently replaced an AI target 19 m north with no label or actor feedback. It now has a persistent 3D label, tells the requesting player where it is and how to test it, removes its old group, and logs spawn plus AAR kill-ledger changes. | FIXED - RETEST REQUIRED |
| FLOW-011 | SafeStart UI appears during and underneath the opening title presentation, and later SafeStart notices stack. | SafeStart authority and protection still begin immediately, but visual controls now wait for the full-pack init/title sequence. Persistent status and transitions share IDC 5300 with token ownership; the obsolete stacked IDC 5301 is hidden. | FIXED - RETEST REQUIRED |
| FLOW-012 | Radio jamming displays two competing panels and restoration is not shown through the preferred EW panel. | Radio and UAV state plus entry/restored transitions now share the bottom-right `ELECTRONIC WARFARE` control (IDC 5310), above the reserved ACRE/TFAR area. Token ownership prevents the watcher from erasing a transition and legacy controls are hidden. | FIXED - RETEST REQUIRED |
| FLOW-013 | SafeStart countdown configuration must use seconds while the live player display remains minutes and seconds. | Public and Zeus inputs now accept whole seconds directly. Persistent and arming displays format the remaining duration as `MM:SS`; no minutes-to-seconds conversion remains in the Zeus handler. | FIXED - RETEST REQUIRED |
| FLOW-014 | SafeStart and EW panels were too large, poorly padded and occupied conflicting screen regions. | SafeStart now owns one internally padded top-centre panel. EW owns one internally padded bottom-right panel above the radio reservation, including entry, active, UAV, RDF, restored and Zeus feedback. | FIXED - RETEST REQUIRED |
| FLOW-015 | Drawn WMP interfaces also emitted Arma hints for the same event. | SafeStart, EW and Economy notification paths no longer use the global hint channel for their owned states. Regression tests reject reintroduction in those visual-owner files. | FIXED - RETEST REQUIRED |
| FLOW-016 | WMP overlays and an empty black frame survived into Arma's death/debrief display. | The 22:14 debrief screenshot showed SafeStart, Economy and orphan panel controls beneath the scoreboard. A WMP-only cleanup now hides owned HUD controls, closes Party/procedure displays and clears presentation tokens on local-player death and mission end without touching Arma or mod controls. | FIXED - RETEST REQUIRED |
| FLOW-017 | Ending a SafeStart countdown still left fire and damage protection active after ENDEX had also run. | The panel was only the visible symptom. SafeStart and ENDEX changed the same ACE safety, fired-handler and `allowDamage` state without recording ownership. The 2026-07-27 03:26 RPT additionally showed ACE safemode unlock receiving an undefined muzzle. Protection now uses source-specific reference claims and stores the exact weapon/muzzle pair WMP safed. SafeStart teardown preserves active ENDEX; ENDEX reset preserves active SafeStart and removes only state WMP owns. | FIXED - RETEST REQUIRED |
| FLOW-018 | Multiple reusable WMP notifications could replace unrelated panels or overlap in one screen region. | Notification ownership is now channel-based. Timed notices queue FIFO, duplicate queued messages coalesce, up to three independent channels stack with measured safe-zone spacing, and overflow waits. Persistent statuses replace only their own channel. Mission makers can define channel placement and explicitly permit or prohibit local player overrides. | FIXED - RETEST REQUIRED |

## Interaction equipment

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| INT-001 | Shared interaction UI has insufficient safe-zone padding and appears almost edge-to-edge. | Screenshots proved that controls were not reserving a protected inset inside Arma's UI-scaled safe zone. The shell now uses the complete `safeZoneX/Y/W/H` rectangle, applies independent X/Y margins and internal padding, and keeps equipment-local coordinates separate from screen coordinates. The runtime validator checks the shared header, objective, timer, Help control, split footer and clipped equipment group for bounds and text overflow. | FIXED - RETEST REQUIRED |
| INT-002 | Briefing objective text clips and the activation button renders as an oversized white default control. | Briefing regions were rebalanced, structured text is measured with `ctrlTextHeight`, and the activation control now uses a themed `RscButton`. | FIXED - RETEST REQUIRED |
| INT-003 | Wire Cut/EOD instruction, probe, cutter, selector and connector controls clip or overlap. | Wire routes terminate before selectors and connector/selector regions are separate. The instruction card now reserves three non-overlapping rows, dynamically shrinks each structured-text row using its actual `ctrlTextHeight`, and gives the accessibility warning enough height. Probe/cutter controls and the loom bay were moved into independent regions. | FIXED - RETEST REQUIRED |
| INT-004 | Interaction screens generally have rough layout, small/illegible text and inconsistent padding. | Static review found that the 40:25 equipment aspect was being imposed directly on Arma's non-square normalized X/Y coordinates, reducing usable height on widescreen displays. The shell now fills 92% by 90% of the visible safe viewport independently. The standalone equipment picker now uses the same visible-safe intersection and equipment-relative padding. All ten procedures still require capture review. | FIXED - RETEST REQUIRED |
| INT-005 | EOD live failure appeared to do nothing and did not visibly drive the source interaction outcome. | Resolver now publishes visible typed feedback before callbacks; live bomb failure explicitly damages the spawned explosive because merely creating IED ammo did not reliably detonate it. | FIXED - RETEST REQUIRED |
| INT-006 | Procedures must support both ACE and vanilla interaction surfaces while remaining discoverable. | Updated player requirement. Interaction equipment is a single-purpose station, so linked ACE and vanilla actions may coexist and must call the same guarded activation path. Server acquisition remains authoritative and rejects delayed duplicate activation. Complex nested systems retain ACE-first/vanilla-fallback behavior. | FIXED - RETEST REQUIRED |
| INT-007 | Earlier mechanics/readability defects: sequence cues too fast/obscured, repair indecipherable, keypad initially unsolvable, EOD shallow, lockpick clipping/speed/tension ambiguity, stacked radio waveforms and Command Entry arrow clipping. | Player observations from focused procedure testing. Several targeted changes exist, but all ten procedures still require the visual/mechanical acceptance matrix. | IN PROGRESS |
| INT-008 | Setup locality allowed clients to broadcast initial/preset interaction variables while installing local actions. | Setup now limits authoritative publication to `isServer`; clients only retain local presentation/action values. Acquisition and resolution log object/actor ownership and still reject wrong-owner, stale and duplicate requests. | FIXED - RETEST REQUIRED |
| INT-009 | Vanilla-only use could fail because lifecycle event emission called CBA unconditionally. | CBA state events are now capability-gated. Public object variables and callbacks remain the default-interaction contract when CBA/ACE are absent. | FIXED - RETEST REQUIRED |
| INT-010 | Interaction fixtures report ACE installation but no activation or acquisition reaches the RPT. | The 18:41 RPT contains 82 local setup records and no ACE invocation, acquisition, RUNNING or resolution record. Action execution now logs separately, stores the actual ACE action/path for runtime inspection, and the audit executes every action condition rather than accepting a Boolean installation flag. End-to-end player activation remains required. | INSTRUMENTED - RETEST REQUIRED |
| INT-011 | Help, briefing, picker, notification and dense equipment text could bypass fitting or runtime validation. | Offline source audit found the Help modal was neither fitted nor visible to the layout validator, the briefing registered only some of its controls, Minesweeper/Keypad/Repair/Sequence structured text used fixed sizes, Lockpick and Repair had narrow dynamic readouts, and the legacy decorator mixed equipment coordinates with raw safe-zone dimensions. Shared plain/structured fitters now constrain width and height, modals hide and restore the underlying face, overlay controls join runtime validation, dense procedure text is fitted after updates, and a static guard rejects unfitted structured text and equipment/safe-zone coordinate mixing. | FIXED - RETEST REQUIRED |
| INT-012 | Every interaction procedure failed to compile after opening. | The 21:32 RPT identified `ctrlSetStyle` in the shared shell. That command does not exist in Arma SQF. The footer now uses a configured structured-text control and the repository-wide SQF validator rejects this runtime token. | FIXED - RETEST REQUIRED |

## Party games

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| PARTY-001 | Leaving Five-Card Draw did not work and may affect other party games. | The shared leave path called every game's departure handler before clearing the seat, so one unrelated error could stop departure. It now dispatches only to the active game. An authority-side audit case executes departure against Draw Poker, Liar's Dice and Connect Four and checks the seat, roster and active state. | FIXED - RETEST REQUIRED |
| PARTY-002 | Party table actions/lobby interactions were suspected of interfering with other actions. | Party records and removes only its own action IDs and preserves unrelated sentinel actions. Tables and seated-player controls now retain linked ACE and vanilla entries for discoverability; both submit the same guarded client request to server authority. | FIXED - RETEST REQUIRED |
| PARTY-003 | Lobby automation repeatedly missed the `OK` confirmation and did not enter the game. | Player observation. Manual audit now requires selecting a slot and confirming in the Arma lobby; in-game party automation must not pretend this validates gameplay. | OPEN |
| PARTY-004 | Preview/game screen was too small to read or interact with and no real features were activated. | The shared fitter included hidden focus controls parked at `[-10,-10]`, inflating source bounds and shrinking visible UI into a bottom-right thumbnail. Off-screen utility controls are now excluded; visible screens fill 95% by 94% of the safe zone and scale text with the fitted canvas. Genuine game-state test cases remain required. | FIXED - RETEST REQUIRED |
| PARTY-005 | Five-Card Draw produced a runtime `ctrlSetText: Type if, expected String` error; the other new games were at risk of the same failure. | The 2026-07-24 13:33 RPT identified an Arma command-precedence error. Every unparenthesized conditional operand for `ctrlSetText` and `ctrlSetBackgroundColor` was corrected in Draw Poker, Liar's Dice and Connect Four. A repository check now rejects this form across all first-party SQF. | FIXED - RETEST REQUIRED |
| PARTY-006 | Shared party safe fitting bounded controls but only reported plain-text overflow. | Every game display reaches the common Escape guard and safe-card pass. The pass now reduces plain-text font height against both actual width and height before recording remaining findings, matching the strengthened Economy UI fitter. Structured-text and dynamic state still require capture review. | FIXED - RETEST REQUIRED |
| PARTY-007 | Party-table setup may be interfering with unrelated `addAction` interactions. | Source review found Party stores and removes only its own per-object IDs; no broad removal was present there. A real cross-feature hazard was found in Economy's purchase-terminal creator, which used `removeAllActions`; that call is removed and first-party use is now rejected. The client audit installs unrelated sentinel actions on the party table and player, reruns both Party installers, and requires both sentinels to survive. | FIXED - RETEST REQUIRED |

## Economy, construction and logistics

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| ECO-001 | Resource collection appeared not to remove the collected crate/object. | Confirmed audit-fixture defect: reset filled capped Parts storage to 500 while the Medium preset cap is 8, so collection correctly retained an unexplained leftover. The fixture now leaves capped stores empty and carries 5 Parts; production feedback explicitly reports partial collection and remaining contents. The 2026-07-27 03:26 instrumentation then checked `isNull` in the deletion request frame and misleadingly logged `deletionQueued=false`. It now confirms after network deletion propagation, retries once if necessary and logs locality/owner details on failure. | FIXED - IN-ENGINE RETEST |
| ECO-002 | Construction HEMTT disappeared after constructing a solar panel. | Source behavior indicates the construction source can be intentionally consumed. The result looked like a bug because no explanation was shown; notification added and needs retest. | EXPECTED - CLARIFIED |
| ECO-003 | Building a crew-served weapon produced an ACE warning. | Player observation. Likely crew/weapon setup integration or locality; RPT evidence must be captured during focused reproduction. | OPEN |
| ECO-004 | Economy, resources, research and construction drawn UIs exceed bounds and do not consistently match WMP branding. | Screenshots showed small prompts stretched independently across the entire safe card: controls remained technically in bounds while navigation buttons became huge slabs. The shared fitter now preserves aspect ratio, caps enlargement, centres the source layout and rejects excessive button height/navigation width. Every drawn Economy screen still requires capture review. | FIXED - RETEST REQUIRED |
| ECO-005 | ACE integrations require systematic coverage. | Player requirement. Audit must check ACE actions first and vanilla parity where supported. | IN PROGRESS |
| ECO-006 | Construction-mode warning and bottom action area clip; the brand strip and other drawn UI lack consistent safe padding. | The construction prompt is routed through the shared visible-safe fitter. Its confirmation control is wider, and the conditional source warning is now the short single-line label `ONE USE: SOURCE IS CONSUMED`. All Economy displays still require capture review. | FIXED - RETEST REQUIRED |
| ECO-007 | Consumption warning appears when Construction Mode is opened from a reusable construction base/research source. | Player screenshot confirms a reusable base receives vehicle-consumption copy. Root cause: the UI created and showed the warning unconditionally, while authoritative deletion already checks `WaldoEcoBuild_IsConstructionVehicle`. UI copy and visibility now use that same flag. | FIXED - RETEST REQUIRED |
| ECO-008 | Approximately half of the Economy Zeus modules appear not to work. | Static audit confirmed reversed custom-class checks in medical/supply crate creation, client-owned crate mutation, unnecessary required payload parameters in Fortify Budget, and convoy execution on the curator rather than the AI group's owner. Crates now use a curator-validated server handler; the other defects are fixed. All 19 Economy callbacks log module name, curator, locality, active state and payload. | FIXED - RETEST REQUIRED |
| ECO-009 | Resource, Economy and related object actions appear unavailable despite server configuration. | Confirmed architecture defect: a broadcast `Published` flag was checked before per-client ACE installation, so a server or earlier client could make later clients skip their local action. Publication and local installation are now separate. Every interface installs locally first; only the server owns JIP publication. Runtime cases verify the four live Economy fixtures have real ACE paths/actions and execute the shared action adapter. | FIXED - RETEST REQUIRED |
| ECO-010 | The Medium preset's Wind Turbine appeared as a red portable lamp. | The configured `Land_WindTurbine_01_F` class does not exist. Construction silently replaced any invalid class with `PortableHelipadLight_01_red_F`, falsely presenting success. The preset now uses `Land_wpp_Turbine_V2_F`. Invalid classes are rejected by configuration, placement, construction UI and server spawn paths; Diagnostics reports the offending build name and class rather than substituting a prop. | FIXED - RETEST REQUIRED |
| ECO-011 | Resource-zone technical anchors appeared as red portable lamps. | Confirmed by the mission maker as intentional: the visible lamp advertises the otherwise abstract authority/Zeus handle. This remains distinct from the invalid Wind Turbine fallback fixed in ECO-010. | EXPECTED - CLARIFIED |
| ECO-010 | Zeus export saved catalogue definitions but not the world layout authored with placement modules. | Resource, Research, Construction and Purchasing now have separate setup-call builders. **Build Setup** translates the settings and placements made through their normal Zeus modules into replayable public calls, in dependency order. Resource side balances and marker visibility are included alongside catalogues and placed fixtures. **Config Copy** retains the portable string workflow. | FIXED - RETEST REQUIRED |
| ECO-011 | Economy Setup Builder removed or hid the other Zeus interfaces when opened; gameplay and Zeus Economy screens lost mouse or keyboard focus. | Attaching authoring controls directly to curator Display #312 left Zeus map, keyboard and mouse handlers active beneath the form. Every Resource, Research, Construction, Purchase, preset, save and purge prompt now owns a transparent modal `RscDisplayEmpty` child. The parent remains visibly present and is restored on close, while all input belongs to the authoring form. A pack-wide guard checks the shared modal owner and every editable Economy prompt's input-target registration. | FIXED - RETEST REQUIRED |
| ECO-012 | Loadout Save module fallback crate should be empty without modifying an explicitly selected target's inventory. | Cargo clearing now exists only in the no-target branch after the module creates its own crate. Existing vehicles, crates and objects are left untouched. | FIXED - RETEST REQUIRED |
| EW-001 | EMP module parameters and detonation announcements cluttered chat. | Module radius/duration now go to RPT only. Physical EMP effects and white-out remain; the affected-player text notice is controlled by `Waldo_EMP_NotifyAffectedPlayers` and defaults off. | FIXED - RETEST REQUIRED |
| LOG-001 | MHQ must be fully fitted with all optional components. | Player requirement. Current runtime assertion reports six synchronized components; manual action/content verification remains. | IN PROGRESS |
| LOG-002 | VVD test equipment is too close to nearby vehicles to exercise deletion safely and unambiguously. | VVD moved to a separate station and the server audit now fails if any unrelated vehicle begins within 25 metres of its pad. | FIXED - RETEST REQUIRED |
| LOG-003 | MHQ shows duplicate deployment actions and cannot deploy. | Full trace found `MHQSetup` executed on both hosted server and client without an idempotency guard, while its ACE completion mutated respawn, marker, lock and visibility state on the interacting client. Setup is now repeat-safe; server state/configuration is one-shot; ACE and vanilla actions are installed once locally; deploy/tear-down requests are owner-validated and executed only on the server. New audit cases check one action of each kind, six deployment parts and authoritative readiness. | FIXED - RETEST REQUIRED |
| LOG-004 | VVD garage action does not open or function. | Two root causes were confirmed: the audit mission omitted `GarageDisplayDefine.hpp`, and server configuration did not publish VVD's local action setup to clients or JIP users. The include is restored and setup now uses one object-keyed JIP publication, with ACE-first local actions and a no-ACE vanilla fallback. | FIXED - RETEST REQUIRED |
| LOG-005 | MHQ has no ACE setup action and its transition sound is absent for nearby multiplayer clients. | ACE object actions are local, but setup previously ran only on whichever machine configured the MHQ; sound ran only on the server. A persistent object-keyed JIP call now installs one ACE tree per client, vanilla is a no-ACE fallback, operation remains server-authoritative, and transition audio is executed locally for nearby listeners. Quartermaster ACE parameter and spawn-authority defects found in the associated chain were also corrected. | FIXED - RETEST REQUIRED |
| LOG-006 | VVD's shared in-use state and preview lifecycle were client-owned and could become stuck. | Static audit found a public mission-namespace lock written by the opening client, no atomic acquisition, unbounded garage initialization waits and an unguarded cleanup array. VVD now acquires a server-owned, actor-bound token before opening the local display; rejects occupied or concurrent requests; expires abandoned locks; releases only matching tokens; keeps preview objects local; bounds initialization; and clears controls on every handled exit. Direct `Waldo_fnc_VVDOpen` calls route through the same acquisition contract. | FIXED - RETEST REQUIRED |

## Diagnostics and current RPT evidence

Latest reviewed RPT: `.qa/pr-review-audit/hosted-profile/arma3_x64_2026-07-27_03-26-22.rpt`.

| ID | Finding | Evidence / diagnosis | Status |
|---|---|---|---|
| DIAG-001 | Diagnostics need pack-wide loaded, disabled, initialised and unavailable distinctions. | Diagnostics now emits structured `LOADED`, `ACTIVE`, `DISABLED`, `UNCONFIGURED`, `UNAVAILABLE` and `ERROR` records and saves the report in `Waldo_Diagnostics_LastReport`. Interaction availability is checked from all ten compiled APIs; the intentionally lazy local challenge registry is reported as detail rather than incorrectly failing the server. | FIXED - RETEST REQUIRED |
| DIAG-002 | Audit BLUFOR has playable slots but an empty scraped loadout pool. | The synthetic `auditLoadoutSQM.hpp` workaround is removed. The real modern, unbinarized `mission.sqm` now contains five distinct custom inventories and is the file included under `MissionSQM`, so loadout, crate and limited-arsenal testing use actual mission data. | FIXED - RETEST REQUIRED |
| DIAG-003 | Configured static-line parachute `rhs_d6_Parachute` is invalid with the loaded audit mods. | Audit configuration now uses vanilla `B_Parachute`; RHS is not part of the required profile. | FIXED - RETEST REQUIRED |
| DIAG-004 | ACRE long-range arrays are empty. | Empty arrays are now reported explicitly as `UNCONFIGURED`, not as an initialization failure. | FIXED - RETEST REQUIRED |
| DIAG-005 | Required mod assertion passed despite player reporting ACE absent from usable features. | RPT confirms `ace=3.21.0.110` and `client/mods/required-loaded` passed. This indicates an integration/action-installation or mission ownership/configuration problem, not that the ACE PBO failed to load. Diagnostics must display both dependency state and subsystem integration state. | OPEN |
| DIAG-006 | Current automated audit gives false confidence. | It completed 14/14 at 16:06:01 while Zeus ownership, client 3D renderer, UI layout and manual workflows were broken. Registration-only checks are being replaced with executable action conditions, adapter invocation, authoritative state changes and full-pack startup evidence. New cases execute Economy's shared action adapter, inspect real ACE action definitions and paths, and mutate server party state through the departure request path. Full feature coverage remains open. | IN PROGRESS |
| DIAG-007 | First diagnostics pass ran before the audit override and falsely reported the optional RHS chute; a later pass was clean. | Audit configuration now executes before production init. Pack defaults preserve already-supplied mission-maker values, so the first and only startup diagnostic sees the vanilla audit chute and enabled Economy state. | FIXED - RETEST REQUIRED |
| DIAG-008 | The 22:12 automated run reported 34/34 PASS but immediately reached a host-death debrief and left public UI visible. | This is recorded as a harness failure rather than a pack pass. Automation is now opt-in and isolated; manual acceptance cannot be satisfied by structured assertions alone. | FIXED - RETEST REQUIRED |
| DIAG-007 | Client interaction-action assertion ran before client fixture setup and falsely reported zero objects. | The 16:52 RPT logged the assertion immediately before all 40 ACE/vanilla action installations. Client readiness is now published only after setup completes and the audit waits for it. | FIXED - RETEST REQUIRED |
| DIAG-008 | Only Economy modules appear in Zeus; normal Waldos modules are absent. | The audit mission bypassed the release root `init.sqf`, which normally calls `Waldo_fnc_ZenInitModules`, while its hosted server happened to register Economy. The audit now registers core modules on every interface, guards against duplicate registration, records exact core/economy counts (15/19), and asserts both families before testing. | FIXED - RETEST REQUIRED |
| DIAG-009 | ACE interaction and party assertions failed even though actions were installed. | The audit executed range-aware conditions while the actor remained at the control console. Tests now temporarily place the actor inside the real action radius and restore the original position, preventing false failures without removing the product's distance guard. | FIXED - RETEST REQUIRED |
| DIAG-010 | Static validation passed an engine-invalid UI command. | Delimiter validation cannot prove command existence. Known Arma-invalid runtime tokens are now rejected repository-wide, full-audit regression tests run in CI, and final acceptance still requires opening the real UI with `-showScriptErrors`. | FIXED - RETEST REQUIRED |
| DIAG-011 | Startup initialized visual systems twice and ran diagnostics before the full pack was ready. | The audit no longer repeats MiniGames, jamming or core ZEN initialization after the real release init. Automatic diagnostics wait for the loadout scrape and pack readiness with a bounded fallback; EW visual sampling waits for the title sequence and its fixture starts outside the player's effect radius. | FIXED - RETEST REQUIRED |
| DIAG-012 | The proven legacy audit mission could not populate mission loadout pools because the scraper only understood modern Eden `Entities/Inventory`. | The repository loadout scraper preserves its modern config path and falls back to `getUnitLoadout` for matching playable units only when no Eden inventories exist. This restores legacy mission support without changing modern mission output. | FIXED - RETEST REQUIRED |
| DIAG-013 | Diagnostics reported `INTERACTIONS LOCAL-ACTIONS ERROR` while the RPT showed all 40 fixtures had both ACE and vanilla actions. | The diagnostic still encoded the superseded ACE-exclusive expectation. It now requires both surfaces when ACE is loaded and vanilla only without ACE, matching the field-equipment integration contract. | FIXED - RETEST REQUIRED |
| DIAG-014 | Economy UI validation returned zero findings for visually unusable, stretched controls. | Bounds-only checks accepted controls that filled the safe card. The shared fitter now preserves source proportions and flags excessive button and navigation-control dimensions, preventing this false pass. | FIXED - RETEST REQUIRED |
| DIAG-015 | Briefing diagnostics emitted `Unknown entity` while otherwise reporting a clean pack. | A raw ampersand in structured briefing text was parsed as an entity. It is escaped, and static coverage now scans every structured briefing line for unescaped entities. | FIXED - RETEST REQUIRED |
| DIAG-014 | Zeus and script setup had no enforceable parameter-parity contract; the jammer omitted bands, initial state and per-emitter 3D visibility, while its tracker omitted label and initial state. | All 34 registered modules now have a checked manifest naming the real public API or documented adapter. Jammer exposes every script parameter plus emitter classname, including a per-emitter curator marker that defaults off; tracker exposes side, label and active state. CI rejects missing registrations, invented API names and required-control regressions. Static wiring passes; actual Zeus dialogs, locality and resulting world state remain in-engine retest requirements. | FIXED - RETEST REQUIRED |

## RPT non-pack observations

- ACRE2 initially logged TeamSpeak pipe errors, then opened the pipe at 16:07:01
  and reported matching plugin/client/server version `2.14.0.1064`. This is now
  healthy for the session.
- ACRE compatibility PBOs skipped optional DLC/RHS/SOG/SPE dependencies that are
  not part of the selected audit profile. These are not WMP defects.
- `Ref to nonnetwork object ... O_Soldier_F` appeared once at 16:11:36 and needs
  correlation with the action being tested before it can be assigned to WMP.

## Acceptance rule

An item moves to `VERIFIED` only after its focused gameplay path succeeds in the
regenerated hosted multiplayer mission and the associated RPT contains no new
first-party script error. Static validation alone is not sufficient for UI,
action visibility, notification timing, Zeus ownership, locality or gameplay.
